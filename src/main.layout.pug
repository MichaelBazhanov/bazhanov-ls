include ./mixins.pug

<!DOCTYPE html>
html(lang="ru")
	head
		meta(charset="UTF-8")
		meta(name="viewport", content="width=device-width, initial-scale=1.0")
		title Portfolio | main page
	body
		//-wrapper -обрезка контента. скрола
		.wrapper
			//- maincontent - отступ до футера
			.maincontent
				block content

		include ./templates/entry.pug
