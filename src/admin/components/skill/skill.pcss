.skill-component {
	display: flex;
	align-items: center;
	justify-content: space-between;

	.percent {
		margin-left: auto;
		margin-right: 45px;
	}

	.buttons {
		display: flex;

		.btn {
			padding: 0 0 0 20px
		}
	}
}
